#include <vector>
#include <string>
#include "token.h"
#include "shunting_yard.h"

using namespace std;

vector<Token> parse(const string& str) {
    return vector<Token>();
};

double compute(const vector<Token>& tokens) {
    return 0;
}